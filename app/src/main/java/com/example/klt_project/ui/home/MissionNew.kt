package com.example.klt_project.ui.home

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity
import com.example.klt_project.databinding.ActivityNewMissionBinding

class MissionNew : AppCompatActivity(){
    private lateinit var binding: ActivityNewMissionBinding

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        binding = ActivityNewMissionBinding.inflate(layoutInflater)
        setContentView(binding.root)

    binding.create.setOnClickListener(
        val gross_weight
        val consignees;
        val consignor;
        val date_of_issues;
        val package_number;
        val pacage_type
        val place_of_destinations;
        val place_of_dispatch
        val product_type;
        val special_intruction;
        val missionID;
    )
    }

}
